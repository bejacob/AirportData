name: Enrich Airports CSV

on:
  workflow_dispatch:    # Manual trigger
  schedule:
    - cron: '0 2 * * *'  # Run daily at 2 AM UTC
  push:
    branches:
      - master
    paths:
      - 'air/data/unknown_codes.txt'

jobs:
  enrich:
    if: >
      (github.event_name == 'push' && github.ref == 'refs/heads/master')
      || (github.event_name == 'workflow_dispatch' && (github.actor == 'mapcat' || github.actor == 'jteresco'))
      || (github.event_name == 'schedule')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Check Python version
        run: python --version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pycountry

      - name: Download OurAirports lookup CSV
        run: curl -o ourairports.csv https://raw.githubusercontent.com/davidmegginson/ourairports-data/master/airports.csv

      - name: Run enrichment script
        run: python air/scripts/enrich_airports.py

      - name: Commit updated airports.csv and discovery logs
        if: github.ref == 'refs/heads/master'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add air/data/airports.csv air/data/recently_indexed.json
          git commit -m "Automatically enriched airports.csv and updated discovery logs" || echo "No changes to commit"
          git push
